# MariaDB credentials used by the database container to initialize itself.
# These values are used to construct the DATABASE_URL in the API's .env file.
MARIADB_ROOT_PASSWORD=your_super_secret_root_password
MARIADB_DATABASE=app_db
MARIADB_USER=app_user
MARIADB_PASSWORD=your_secret_db_password
